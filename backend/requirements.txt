# ── Web Framework ──
fastapi==0.136.1
uvicorn[standard]==0.44.0

# ── Database ──
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.18.4
greenlet==3.3.2

# ── Auth ──
python-jose[cryptography]==3.5.0
argon2-cffi==25.1.0

# ── Security dependency pins (CVE fixes) ──
# pyjwt >= 2.12.0 — CVE-2026-32597: JWT 'crit' header bypass (HIGH)
pyjwt>=2.12.1
# cryptography >= 46.0.6 — CVE-2024-12797, CVE-2026-26007, CVE-2026-34073 (OpenSSL + EC point validation)
cryptography>=46.0.7
# wheel >= 0.46.2 — CVE-2026-24049: path traversal in wheel unpack (HIGH)
wheel>=0.46.2
# urllib3 >= 2.6.3 — CVE-2025-50181/82, multiple redirect/decompression vulns
urllib3>=2.6.3
# requests >= 2.33.0 — CVE-2024-47081, CVE-2026-25645: .netrc leak, temp file
requests>=2.33.1

# ── Security ──
slowapi==0.1.9
pycryptodome==3.23.0

# ── Cache ──
redis[hiredis]==7.4.0

# ── HTTP Client (for Piston) ──
httpx==0.28.1

# ── Docker SDK (for lab management) ──
docker==7.1.0

# ── Settings ──
pydantic-settings==2.13.1

# ── Validation ──
email-validator>=2.0.0

# ── Utilities ──
python-multipart==0.0.24

# ── SSE ──
sse-starlette==3.3.4

# ── WebSocket proxy (noVNC) ──
websockets>=12.0,<17.0
